# Spring Boot 2.x → 4.1.0 直接アップグレード要点（非公式・強行ルート）

> **前提の確認**: Springは 2.x → 3.x → 4.x の**段階的移行を公式に推奨**しており、2.xから4.xへの直接ジャンプはサポート外・実績の乏しいルート。本ドキュメントは [[SB2-3]]・[[SB3-4]] の内容を踏まえつつ、それでも直接4.1.0へ強行する場合に、ビルドエラーを一つずつ潰していく現実的な手順としてまとめたもの。段階移行より**手戻り・切り戻しのリスクが高い**ことを前提に読むこと。

## 0. 強行ルートの心構えとリスク

- 2.x→3.xの変更（javax→jakarta、Security 6のAPI全面変更等）と、3.x→4.xの変更（Jackson 2→3、starterモジュール分割、Jakarta EE 11等）が**すべて同時に襲ってくる**。エラーメッセージだけでは「2→3由来の問題」か「3→4由来の問題」か判別しにくいため、[[SB2-3]]と[[SB3-4]]を常に手元に置いて突き合わせながら進める。
- OpenRewriteの移行レシピは **2.x→3.0** 用と **3.x→4.0** 用が別々に提供されている。2.x→4.1へ一気に適用できるレシピは存在しない前提で、**両方のレシピを順番に適用**するのが現実的（一発で当てようとしない）。
- 強行する場合でも、**必ず専用ブランチ + こまめなコミット**で進める。「ビルドが通った時点」を1コミットの単位にし、いつでも直前の緑の状態に戻れるようにする（`git bisect`的に原因切り分けできる状態を維持）。
- 本番投入前に、最低でも一度は**3.5.x相当の中間状態でテストが通ることを確認**できると安全性が大きく上がる（後述、任意ステップとして推奨）。

## 1. 全体戦略：ビルドエラーを潰す「順番」が重要

ビルドエラーは以下の順で解消すると、後工程のノイズが減る。**下の階層が解決するまで上の階層のエラーは正しく評価できない**（前提が壊れたままだと無関係なエラーが大量に出るため）。

```
Lv0: JDKバージョン（17〜21以上、jOOQ利用時は21以上）
Lv1: ビルドツール本体（Gradle 8.14+/9系、Mavenラッパー）
Lv2: BOM/親POM・プラグインのバージョン（spring-boot-starter-parent 等を4.1.0へ）
Lv3: starter名・依存座標の変更（jakarta化、モジュール分割）
Lv4: コンパイルエラー（削除/改名されたクラス・アノテーション）
Lv5: 起動時エラー（自動構成、プロパティ名変更）
Lv6: テストコードのコンパイル・実行エラー
Lv7: 実行時の意味的な差異（Jacksonの出力形式など、動くが結果が違う）
```

Lv0〜Lv3は「一括で書き換えてしまい、大量のコンパイルエラーを出してから一つずつ潰す」方式が効率的。Lv4以降は**エラーメッセージ単位のホワックアモグラ**になるため、後述の対応表を使う。

## 2. Step 0: 事前準備（強行前でも最低限やること）

- [ ] 現行2.xでの全テストが緑であることを確認し、ベースラインを記録。
- [ ] 作業用ブランチを作成。
- [ ] JDKを 21以上（推奨: 25、jOOQ利用時は21必須）に切り替え。
- [ ] Gradleは8.14以上（推奨9系）、Mavenは最新へ。
- [ ] 依存関係グラフを出力しておく（`./gradlew dependencies` / `mvn dependency:tree`）。強行後に「何が引っ張られているか」の差分比較に使う。
- [ ] Springfox/Swagger2、Spring Cloud Sleuth、Undertow、`javax.*`直参照の内製ライブラリなど、[[SB2-3]]・[[SB3-4]]記載の**代替手段が必須な依存**を先にリストアップしておく。

## 3. Step 1: ビルド定義を4.1.0向けに一括書き換え

一度に以下を書き換えて、まず「ビルドが盛大に壊れた状態」を作る（これが正常）。

- `spring-boot-starter-parent` / `org.springframework.boot`プラグインを **4.1.0** に変更。
- Spring Cloud BOMを利用している場合は4.1.0対応版に変更（対応バージョンが存在するか事前確認。存在しない場合はSpring Cloud依存を一時的に外す判断も必要）。
- starter名を新モジュール名へ機械的に置換:
  - `spring-boot-starter-web` → `spring-boot-starter-webmvc`
  - `spring-boot-starter-web-services` → `spring-boot-starter-webservices`
  - WARデプロイなら `spring-boot-starter-tomcat` → `spring-boot-starter-tomcat-runtime`
  - Flyway/Liquibase/Securityテストなど暗黙依存だったものを明示的に追加（`spring-boot-starter-flyway`, `spring-boot-starter-liquibase`, `spring-boot-starter-security-test`）
- Undertowを使っている場合はこの時点でTomcat/Jettyへ切り替え（4系で完全廃止のため待っても無駄）。
- 迷ったら一時的に `spring-boot-starter-classic` を挟んで様子を見る選択肢もあるが、最終的な撤去が必要な点は忘れないこと。

この時点でビルドは大量のエラーを吐く。これは想定通り。

## 4. Step 2: OpenRewriteによる機械的置換（2段階）

手作業での置換量を減らすため、以下を**順番に**適用する。

1. `org.openrewrite.java.spring.boot3.UpgradeSpringBoot_3_0` 系レシピ → javax→jakarta化、Boot3時点のAPI変更を機械的に反映。
2. Spring Boot 4向けのOpenRewrite/Moderneレシピ → starter名変更、Jackson2→3のクラス名変更などを反映。

自動変換後も**必ず差分を目視確認**する。特にjakarta化は「文字列としての`javax`」まで置換してしまうケース（ログメッセージやコメント）があるため、意図しない置換がないか確認。

## 5. Step 3: コンパイルエラー対応表（頻出パターン）

Lv3のビルド定義修正が終わったら、以降はコンパイルエラーを一つずつ潰す。頻出するエラーと対応を以下にまとめる（上から順に潰すと連鎖的に解決しやすい）。

| エラー内容（症状） | 原因 | 対応 |
|---|---|---|
| `package javax.persistence does not exist` 等 | jakarta名前空間未移行 | `javax.*` → `jakarta.*` へ全置換（[[SB2-3]] §2） |
| `cannot find symbol: WebSecurityConfigurerAdapter` | Security 6で削除済み | `SecurityFilterChain` Bean方式へ書き換え（[[SB2-3]] §4） |
| `cannot find symbol: antMatchers` | Security 6/7で削除 | `requestMatchers()` へ置換 |
| `package com.fasterxml.jackson.databind does not exist`（一部クラス） | Jackson3でグループID変更 | 影響クラスは `tools.jackson.*` を確認し import修正。`ObjectMapper`カスタマイズBeanは`JsonMapper`/`XmlMapper`へ書き換え（[[SB3-4]] §3） |
| `cannot find symbol: @JsonComponent` | Jackson3で改名 | `@JacksonComponent` へ置換 |
| `cannot find symbol: Jackson2ObjectMapperBuilderCustomizer` | 改名 | `JsonMapperBuilderCustomizer` へ置換 |
| `cannot find symbol: @MockBean` / `@SpyBean` | Boot4で完全削除 | `@MockitoBean` / `@MockitoSpyBean` へ置換（機械的一括置換が可能） |
| `cannot find symbol: HttpMessageConverters` | 非推奨→実質置き換え対象 | `ClientHttpMessageConvertersCustomizer` / `ServerHttpMessageConvertersCustomizer` へ移行 |
| starter依存が解決できない（`Could not find artifact spring-boot-starter-web`） | starter名変更 | `spring-boot-starter-webmvc` 等の新名称へ（[[SB3-4]] §4） |
| Undertow関連のクラスが見つからない | 組み込みサーバーとして廃止 | Tomcat/Jettyへ切り替え |
| `spring.factories` の自動構成が読み込まれない | 登録方式変更（2→3で既に変更） | `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` に統一されているか確認 |

## 6. Step 4: 起動時エラー対応（コンパイルは通ったが起動しない）

- `spring-boot-properties-migrator` を**一時的に**依存追加し、起動ログで非推奨/削除プロパティを洗い出す（本番投入前に必ず外す）。
- 代表的なプロパティ移動:
  - `spring.session.redis.*` → `spring.session.data.redis.*`
  - `spring.session.mongodb.*` → `spring.session.data.mongodb.*`
  - `spring.data.mongodb.*`（一部） → `spring.mongodb.*`
  - `spring.kafka.retry.topic.backoff.random` → `spring.kafka.retry.topic.backoff.jitter`
- Spring Batchを使っている場合、**起動はするがメタデータがDBに書かれなくなる**トラップに注意（デフォルトが非DBモードに変更）。既存運用を維持するなら `spring-boot-starter-batch-jdbc` を明示追加。
- Spring Cloud Sleuthを使っていた場合は起動時にBean解決エラーになりやすい。Micrometer Tracingベースの構成へ全面置き換え（[[SB2-3]] §6）。
- Elasticsearchの`RestClient`自動構成に依存している場合、`Rest5Client`への置き換えが必要になることがある。

## 7. Step 5: テストコードの修正

- `@MockBean`/`@SpyBean` → `@MockitoBean`/`@MockitoSpyBean` への一括置換（IDEのリファクタ機能推奨）。
- `@SpringBootTest`でMockMvc/WebTestClient/TestRestTemplateが自動注入できなくなるため、`@AutoConfigureMockMvc` / `@AutoConfigureTestRestTemplate` を明示的に追加。
- `@WithMockUser`等のSecurityテストアノテーションを使っている場合、`spring-boot-starter-security-test` を追加しないとテストコンパイルが通らない。
- JUnit4が残っている場合はこの機会にJUnit5へ統一（周辺エコシステムがJUnit5前提のため）。
- `MockitoTestExecutionListener`削除の影響で`@Mock`/`@Captor`が効かなくなったら、Mockito本家の`MockitoExtension`を使う。

## 8. Step 6: ビルド・起動・テストが通った後の「動くが結果が違う」系の確認（Lv7）

ここまで来てビルド・起動・テストがグリーンになっても、**仕様として挙動が変わっている箇所**がある。ビルドエラーには出ないため見落としやすい。

- **Jacksonの日付シリアライズ形式が反転**（タイムスタンプ数値 → ISO-8601文字列）。API契約・フロントエンドへの影響を確認。暫定的には `spring.jackson.use-jackson2-defaults=true` で吸収可能。
- **`PropertyMapper`がnull値をマッピングしなくなった**。既存コードで意図的にnullを伝播させていた箇所があれば`.always()`が必要。
- **DevToolsのLiveReloadがデフォルト無効**。開発体験に影響するなら明示的に有効化。
- **Logbackのデフォルト文字コードがUTF-8統一**。文字化けしていた既存ログ設定に依存していた場合は動作が変わる可能性。
- Jackson例外階層の変更（`JacksonException`が非チェック例外化）により、既存の`catch (IOException e)`がJacksonの例外を捕捉しなくなるケースがある。ログにcatchされない例外が出ていないか確認。

## 9. 強行ルート特有の追加リスク

- **サードパーティ/社内ライブラリの対応状況**: 2.x時代の古いライブラリはjakarta化にもJackson3にも対応していないことが多い。3.xを経由しないため、「3.xでは動いていたが4.xで壊れる」ライブラリと「そもそも2.xの時点で3.x非対応だった」ライブラリが混在して見え、切り分けが難しくなる。依存ライブラリは早めに最新版へのアップデート可否を個別確認する。
- **Spring Cloud**: 2.xで使っていたSpring Cloudバージョンから4.1.0対応版まで一気に上げる必要があり、Spring Cloud側の破壊的変更（Sleuth廃止等）も同時発生する。Spring Cloudを使っている場合は特に慎重に。
- **切り分けの困難さ**: 一つのコンパイルエラーが「2→3の変更」なのか「3→4の変更」なのか即断できないことが多い。エラーが出たら、まず[[SB2-3]]・[[SB3-4]]のどちらに該当する変更か照合してから対応表（本書Step 3）を参照する癖をつけると迷いが減る。
- **段階検証という保険**: どうしても直接ジャンプが必要でも、ローカルやCI上だけでも一度 **Spring Boot 3.5.x相当のブランチ**でビルド・テストを通してから4.1.0へ進むと、問題切り分けの負荷が大きく下がる（強行はするが、検証は分割するという妥協案）。

## 10. 参考

- [[SB2-3]]（Spring Boot 2.x → 3.x 要点）
- [[SB3-4]]（Spring Boot 3.x → 4.x 要点）
- [Spring Boot 4.0 Migration Guide](https://github.com/spring-projects/spring-boot/wiki/Spring-Boot-4.0-Migration-Guide)
- [Spring Boot 4.1 Release Notes](https://github.com/spring-projects/spring-boot/wiki/Spring-Boot-4.1-Release-Notes)
