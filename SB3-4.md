# Spring Boot 3.x → 4.x バージョンアップ要点

> Spring Boot 4.0 は 2025年11月にSpring Framework 7.0と共にリリース済み（GA）。本ドキュメントはリリース後の公式移行ガイド・実運用レポートを基にした最新情報。

## 0. 移行の全体方針

- いきなり4.0に上げず、まず **最新の Spring Boot 3.5.x** に上げ、非推奨(deprecated)警告を解消してから移行するのが公式推奨。
- 3.x → 4.0 は **115件の破壊的変更** が報告されており、影響度別に整理すると把握しやすい:
  - **Tier1（ビルド不可）**: 約42件 — Gradleバージョン要件、starter名変更、Undertow削除など
  - **Tier2（ビルドは通るが実行時失敗）**: 約24件 — javax名前空間の残存、RestTemplate自動構成削除、`@MockBean`/`@SpyBean`削除など
  - **Tier3（動くが結果が異なる）**: 約19件 — Jacksonの日付シリアライズ形式のデフォルト反転など
- 移行支援には `spring-boot-properties-migrator`（プロパティ診断）や OpenRewrite の Spring Boot 4 移行レシピ、Moderneのマイグレーションツールが利用可能。

## 1. Java / Kotlin / ビルドツール要件

- **Java 17以上が必須**（変更なし。ただしJava 25への一級対応が追加され、最新LTS利用が推奨）。
- **Kotlin 2.2以上**が必要。
- **GraalVM Native Image**利用時は v25以降が必要。
- **Gradle 8.14以上、または9系**が必須（Gradle 9系推奨）。
- **Maven**: `spring-boot-starter-parent` を4.x系へ。

## 2. Jakarta EE 11 ベースラインへの移行

Spring Boot 4は **Jakarta EE 11 / Servlet 6.1 baseline** に移行し、Spring Framework 7.0が必須。

- **Hibernate ORM 7.x** への更新が必要（Jakarta EE 11対応）。
- **Bean Validation 3.1** 対応版へ。
- **Tomcat 11** ベース。
- **Undertowは組み込みサーバーとして完全廃止**（Servlet 6.1非互換のため）。Undertow利用者はTomcatまたはJettyへの切り替えが必須。

## 3. Jackson 2 → Jackson 3 への大規模移行（最重要ポイント）

3→4移行における最大の変更点。

- **グループIDが変更**: `com.fasterxml.jackson` → `tools.jackson`（`jackson-annotations`を除く）。
- **エントリポイントの変更**: `ObjectMapper` Beanの定義だけでは自動構成の置き換えに不十分。JSON用に **`JsonMapper`**、XML用に **`XmlMapper`** を自動構成するようになったため、カスタマイズ時は `JsonMapper`/`XmlMapper` Beanを定義する。
- **`JsonMapper`は不変(immutable)なビルダーAPI**を持ち、Java 17+向けの妥当なデフォルトを標準搭載（`jackson-datatype-jsr310`を別途登録しなくても`LocalDateTime`等が扱える）。
- **デフォルトのシリアライズ形式が反転**: Jackson2は`WRITE_DATES_AS_TIMESTAMPS=true`（例: `1699257000000`）がデフォルトだったが、Jackson3では`false`（ISO-8601文字列、例: `"2025-11-06T05:30:00"`）がデフォルトに。既存クライアントとの互換性に注意。
- **例外階層の変更**: Jackson2では`IOException`をcatchすれば良かったが、Jackson3の`JacksonException`は**非チェック例外**（`RuntimeException`継承）になったため、catch節の見直しが必要。
- **アノテーション/クラス名の変更**:
  - `@JsonComponent` → `@JacksonComponent`
  - `@JsonMixin` → `@JacksonMixin`
  - `JsonObjectSerializer` → `ObjectValueSerializer`
  - `Jackson2ObjectMapperBuilderCustomizer` → `JsonMapperBuilderCustomizer`
- **設定プロパティの移動**: 一部プロパティが `spring.jackson.json.read` / `spring.jackson.json.write` 配下に整理。
- **移行支援プロパティ**: `spring.jackson.use-jackson2-defaults=true` を設定すると、自動構成される`JsonMapper`がSpring Boot 3.x（Jackson2）に近いデフォルト挙動になる（暫定的な互換維持に有用）。
- **後方互換モジュール**: どうしてもJackson2を使い続ける必要がある場合、非推奨予定の `spring-boot-jackson2` モジュールが用意されている（恒久対応ではなく移行期間用）。
- 注意: **Jersey 4.0はまだJackson 3非対応**のため、Jersey利用時はJSON処理に`spring-boot-jackson2`が必要。

## 4. モジュール構成・starter名の変更（分割モジュール化）

Spring Bootの内部構造が全面的にモジュール分割され、jarがより小さく明確な単位に再編された。

- 命名規則: モジュールは `spring-boot-<technology>`、starterは `spring-boot-starter-<technology>`、テストは `spring-boot-starter-<technology>-test`。
- 代表的な名称変更:
  - `spring-boot-starter-web` → **`spring-boot-starter-webmvc`**
  - `spring-boot-starter-web-services` → **`spring-boot-starter-webservices`**
  - AOPスターターが改名（AspectJサポート向け）
  - `spring-boot-starter-tomcat` は**WARデプロイ時**は `spring-boot-starter-tomcat-runtime` に切り替えが必要
- **Flyway / Liquibase** は自動構成の暗黙依存が廃止され、`spring-boot-starter-flyway` / `spring-boot-starter-liquibase` の明示的追加が必須に。
- **`@WithMockUser`などSecurityテスト機能**を使う場合は `spring-boot-starter-security-test` の追加が必要。
- 過渡期の互換性維持には `spring-boot-starter-classic` が用意されている（段階移行用、最終的には廃止予定）。

## 5. テスト関連の変更（影響が大きい）

- **`@MockBean` / `@SpyBean` が完全削除**。Mockito本家の **`@MockitoBean` / `@MockitoSpyBean`** へ置き換え必須。
- **`@SpringBootTest`の自動構成範囲が縮小**:
  - MockMvcの自動サポートが削除 → `@AutoConfigureMockMvc` の明示的追加が必要
  - `WebTestClient` / `TestRestTemplate` も自動提供されなくなり → `@AutoConfigureTestRestTemplate` 等の追加が必要
- `MockitoTestExecutionListener`（3.4で非推奨化済み）が削除。`@Mock`/`@Captor`が期待通り動かない場合はMockito本家の `MockitoExtension` を使用。
- `@PropertyMapping` が `org.springframework.boot.test.autoconfigure.properties` → `org.springframework.boot.test.context` パッケージへ移動。
- **Spock統合が廃止**（Spock がGroovy 5未対応のため）。

## 6. Spring Security 7.0 対応

- Spring Framework 7.0連動でSpring Security 7.0が必須。
- テスト関連（`@WithMockUser`等）は前述の通り `spring-boot-starter-security-test` の明示追加が必要になった点に注意。

## 7. 設定プロパティの変更

- **Spring Session**:
  - `spring.session.redis.*` → `spring.session.data.redis.*`
  - `spring.session.mongodb.*` → `spring.session.data.mongodb.*`
- **MongoDB**: Spring Data MongoDBを使わない構成の場合、多くのプロパティが `spring.data.mongodb.*` → `spring.mongodb.*` に移動（`uri`, `database`, `username`など）。
- **Kafka**: `spring.kafka.retry.topic.backoff.random` → `spring.kafka.retry.topic.backoff.jitter`。Spring Kafka / Spring AMQPは Spring Retry から **Spring Framework自体のリトライ機能**へ移行。
- 変更点の洗い出しには `spring-boot-properties-migrator` を一時的に追加して起動時診断するのが有効（本番投入前に除去）。

## 8. その他の重要な変更点

- **`HttpMessageConverters`が非推奨化**。`ClientHttpMessageConvertersCustomizer` / `ServerHttpMessageConvertersCustomizer` を使った個別カスタマイズへ移行。
- **Elasticsearch**: 組み込みの`RestClient`が廃止され、新しい **`Rest5Client`** の自動構成に置き換え。
- **Spring Batch 6.0**: **データベース無しでの動作がデフォルト**に変更。既存DBにメタデータを保存する従来動作を維持するには `spring-boot-starter-batch-jdbc` への切り替えが必要（アップグレード後に既存メタデータテーブルへ書き込まれなくなる点に要注意）。
- **DevToolsのLiveReloadがデフォルト無効化**。使う場合は `spring.devtools.livereload.enabled=true` を明示設定。
- **`PropertyMapper`のデフォルト挙動変更**: null値に対してマッピングを実行しなくなった。従来通りnullもマッピングしたい場合は `.always()` を使用。
- **Logbackのデフォルト文字コードがUTF-8に統一**。
- **Pulsar Reactive自動構成が削除**（Spring Pulsarのリアクティブ機能自体が廃止）。
- **実行可能Uber Jarの起動スクリプト機能（embedded launch scripts）が廃止**。Gradleのapplication pluginなど代替手段の利用が必要。
- **RestTemplate自動構成が削除**（Tier2の破壊的変更）。`RestClient`や`WebClient`ベースの構成への移行を推奨。
- **OAuth 2.0 password grantのサポート削除**。
- **javax.annotation / javax.inject 名前空間が完全非サポート**（3.x移行時にjakarta化済みのはずだが、取りこぼしがあれば実行時エラーになる）。

## 9. 依存プロジェクトのバージョン対応表（目安）

| プロジェクト | Spring Boot 4.0対応バージョン |
|---|---|
| Spring Framework | 7.0 |
| Spring Security | 7.0 |
| Spring Data | 2025.1 |
| Spring Integration | 7.0 |
| Spring Batch | 6.0 |
| Micrometer | 1.16 |
| Hibernate ORM | 7.x |

## 10. 新機能（移行時に検討したいポジティブな変更）

- **API Versioning** と **HTTP Service Client（宣言的HTTPクライアント）** のサポートが本体に統合。
- **JSpecifyによるnull安全性**がポートフォリオ全体で改善。
- **`spring-boot-starter-opentelemetry`** starterが新規追加され、OTLP経由のメトリクス・トレースエクスポートが簡素化。
- **Redis Static Master/Replica構成**の自動構成対応（Lettuceのみ、`spring.data.redis.masterreplica.nodes`）。

## 11. 移行時の落とし穴・注意点

- Jacksonのデフォルト日付形式反転は**フロントエンドやAPI契約に直接影響**するため、`use-jackson2-defaults`で一旦吸収しつつ計画的に移行するか、契約側の対応可否を先に確認する。
- `@MockBean`/`@SpyBean`廃止はテストコード全体に影響するため、事前に使用箇所を棚卸しして`@MockitoBean`へ機械的置換する（IDEの一括置換 or OpenRewriteレシピ推奨）。
- Undertowを使っている場合はサーバー切り替えが必須（性能特性が変わるため負荷試験を再実施）。
- Spring Batchのデフォルト非DB化は、**既存運用でジョブ実行履歴をDBで追跡している場合に気づかず失う**リスクがあるため、`spring-boot-starter-batch-jdbc`の要否を必ず確認。
- starter名変更（`spring-boot-starter-web`→`webmvc`等）はビルド定義の機械的修正が必要だが、社内BOM/親POMを使っている場合はそちらの更新も忘れずに。
- 移行の中間ステップとして `spring-boot-starter-classic` を使う場合も、最終的な廃止を見据えて計画的に新モジュール構成へ移行すること。

## 12. 参考情報

- [Spring Boot 4.0 Migration Guide (公式Wiki)](https://github.com/spring-projects/spring-boot/wiki/Spring-Boot-4.0-Migration-Guide)
- [Spring Boot 4.0 Release Notes](https://github.com/spring-projects/spring-boot/wiki/Spring-Boot-4.0-Release-Notes)
- [Spring Boot 4.0.0 available now (spring.io公式ブログ)](https://spring.io/blog/2025/11/20/spring-boot-4-0-0-available-now/)
- [Introducing Jackson 3 support in Spring (spring.io公式ブログ)](https://spring.io/blog/2025/10/07/introducing-jackson-3-support-in-spring/)
- [Spring Boot 4.1 Release Notes](https://github.com/spring-projects/spring-boot/wiki/Spring-Boot-4.1-Release-Notes)（4.1系の追加変更点も要確認）
