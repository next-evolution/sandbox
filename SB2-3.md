# Spring Boot 2.x → 3.x バージョンアップ要点

## 0. 移行の全体方針

- **いきなり3.xに上げない**。まず現行を **Spring Boot 2.7.x（最新パッチ）** に上げ、非推奨警告(deprecation warning)を全て解消してから3.0以降へ移行するのが安全。
- 2.7 → 3.x は破壊的変更が多いメジャーアップグレードのため、CI・テストカバレッジを確保した上で段階的に進める。
- 移行支援ツール **OpenRewrite の `org.openrewrite.java.spring.boot3.UpgradeSpringBoot_3_0`**（Moderne/Spring公式レシピ）を使うと、javax→jakartaの置換など機械的な変更を自動化できる。

## 1. 前提条件（必須要件の変更）

- **Java 17 以上が必須**（2.xはJava 8以上でOK）。ビルド・実行環境のJDKを17系（LTS）へ更新する必要がある。
- **Spring Framework 6.x** ベースになる（内部的にSpring Boot 3.xはSpring 6を使用）。
- 主要ミドルウェアの対応バージョン確認が必要:
  - Tomcat 10.1 / Jetty 11 / Undertow 2.3 以降
  - Hibernate ORM 6.1 以降
  - Jakarta EE 9/10（Servlet 5.0/6.0, JPA 3.0/3.1 等）

## 2. javax.* → jakarta.* 名前空間の移行（最大の変更点）

Jakarta EE 9で `javax.*` パッケージが `jakarta.*` に改名されたため、Spring Boot 3.xでは全面的に対応が必要。

- 影響範囲の例:
  - `javax.persistence.*` → `jakarta.persistence.*`
  - `javax.servlet.*` → `jakarta.servlet.*`
  - `javax.validation.*` → `jakarta.validation.*`
  - `javax.annotation.*` → `jakarta.annotation.*`
  - `javax.transaction.*` → `jakarta.transaction.*`
- 自前コードのimport文はもちろん、**使用しているサードパーティライブラリもjakarta名前空間対応版へアップグレードが必須**（対応していない古いライブラリは動作しない）。
- 上記OpenRewriteレシピで機械的に置換可能だが、置換後は全ビルド・全テストの再確認が必須。

## 3. 主要依存ライブラリのアップグレード

- **Hibernate 5.x → 6.x**: 内部実装変更あり。HQLの挙動やDDL自動生成の一部が変わるケースがあるため、DB周りのテストを重点的に確認。
- **Jackson**: 2.13以降の対応版へ。
- **Spring Security 5.x → 6.x**（後述）。
- **Spring Data**: メジャーバージョンアップに伴うリポジトリAPIの変更点を確認。
- **Spring Cloud** 利用時は Spring Cloud のバージョンも Spring Boot 3.x 対応版（2022.0.x系以降）へ揃える。
- **Lombok** など javax.annotation.Generated 等を参照するツールもjakarta対応版か確認。

## 4. Spring Security 6.x への対応

- `WebSecurityConfigurerAdapter` が**完全に削除**。`SecurityFilterChain` Bean定義方式へ全面移行が必要（2.7時点で既に非推奨化されており、先に対応しておくと楽）。
- `antMatchers()` / `mvcMatchers()` / `regexMatchers()` が削除され、**`requestMatchers()`** に統一。
- 認可設定のラムダDSL化が推奨（`authorizeRequests()` → `authorizeHttpRequests()`）。

## 5. application.properties / yml の変更点

- 一部プロパティ名が変更・削除されている。移行時は起動ログの `ConfigurationPropertiesReportEndpoint` や `spring-boot-properties-migrator` を一時的に依存追加すると、非推奨プロパティを検出できる（本番運用前に外すこと）。
- 代表例:
  - `spring.factories` による自動構成登録は廃止され、`META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` に統一。
  - Actuatorの一部プロパティ・エンドポイントIDの変更。

## 6. Actuator / Observability 関連の変更

- **Micrometer Tracing** が導入され、旧 **Spring Cloud Sleuth は非推奨・統合終了**。分散トレーシングを使っている場合は Micrometer Tracing + Zipkin/OTel Bridge へ移行が必要。
- `management.metrics.*` 系プロパティやメトリクス名の一部変更あり（例: Tomcatメトリクス名など）。
- Actuatorのヘルスインジケータ・エンドポイントの互換性を確認。

## 7. テスト関連

- JUnit 4 を使っている場合は先にJUnit 5へ移行推奨（Boot 3自体はJUnit4も動くが、周辺エコシステムがJUnit5前提になりつつある）。
- `@MockBean`/`@SpyBean` 等の挙動確認（Mockito/JUnitバージョンとの整合性）。

## 8. ビルドツール関連

- **Maven**: `spring-boot-starter-parent` を3.x系に更新。Java 17ターゲットへ `maven-compiler-plugin` 設定変更。
- **Gradle**: Gradle 7.5以上（推奨8系）が必要。`org.springframework.boot` プラグインを3.x系へ、`sourceCompatibility`/`targetCompatibility` を17へ。
- CI環境（Docker baseイメージ等）のJDKも17以上に更新。

## 9. GraalVM Native Image 対応（任意だが目玉機能）

- Spring Native（サードパーティプロジェクト）が**Spring Boot本体に統合**され、`spring-boot-buildtools`や`org.graalvm.buildtools`プラグインでネイティブイメージビルドが標準サポートに。
- ネイティブイメージ化する場合はリフレクション・プロキシ利用箇所に `@RegisterReflectionForBinding` 等のヒント定義が必要になることがある。

## 10. 移行時の落とし穴・注意点

- Springfox（Swagger2）は jakarta 非対応のため動作しない。**springdoc-openapi** への切り替えが実質必須。
- Bean Validationのアノテーションが `jakarta.validation.constraints.*` に変わるため、DTOのimportを全置換。
- 古いJava EEベースの内製ライブラリ・社内共通部品がある場合、jakarta移行の影響が波及するので棚卸しが必要。
- Docker/Buildpacksベースイメージも Java 17対応のものへ更新。
- 段階移行時は「2.7でdeprecation警告ゼロ」を中間ゴールにすると、3.0移行時の差分が最小化できる。

## 11. 参考にすべき公式情報

- Spring Boot 3.0 Migration Guide（Spring公式Wiki）
- Spring Framework 6.0 What's New
- Spring Security 6.0 Migration Guide
- OpenRewrite Spring Boot 3 migration recipes
